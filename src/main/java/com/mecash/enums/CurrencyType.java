package com.mecash.enums;

/**
 * Represents the two currencies supported by the platform.
 *
 * Exchange rates are applied when funds are transferred between different currencies.
 */
public enum CurrencyType {
    A,
    B
}